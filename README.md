# Git и командная строка — Конспект по темам

## 📁 Навигация по файловой системе (bash)

```bash
pwd                 # показать текущую директорию
ls                  # список файлов и папок
ls -a               # включая скрытые файлы (например, .git)
ls ~                # содержимое домашней директории
ls ..               # содержимое родительской директории
cd ~                # перейти в домашнюю директорию
cd имя_папки        # перейти в указанную директорию
cd ..               # перейти на уровень выше
cd /c/Users/wayp1/Desktop  # абсолютный путь
```

## 📄 Работа с файлами и папками

```bash
touch file.txt                                # создать файл
touch ../../file.txt                          # создать файл на два уровня выше
touch first-project/data.txt first-project/table.csv  # создать несколько файлов

mkdir dir_name                                 # создать директорию
mkdir -p dir1/dir-inside/dir-deeper-inside     # создать вложенные директории
mkdir ~/my-git-projects                        # создать папку в домашней директории

cp index.html dir/         # копировать файл в директорию
mv data.txt Desktop/test   # переместить файл (или переименовать)
cat myfile.txt             # показать содержимое файла

rm example.txt             # удалить файл
rmdir some-dir             # удалить пустую директорию
rm -r some-dir             # удалить директорию с содержимым
```

## 🔗 Комбинирование команд

```bash
mkdir second-project && cd second-project && touch index.html style.css
# последовательно: создаём папку, переходим в неё, создаём файлы
```

## ⚙️ Настройка Git

```bash
git config --global user.name "User Namovich"       # задать имя
git config --global user.email username@yandex.ru   # задать email
cat ~/.gitconfig                                     # просмотреть конфиг
git config --list                                    # список всех настроек
```

## 🧱 Репозиторий Git

```bash
git init                      # инициализировать репозиторий
rm -rf .git                   # удалить репозиторий ("разгитить")
```

> 🔸 Ключи:
>
> * `-r` — рекурсивно
> * `-f` — принудительно (без подтверждений)

## 📌 Работа с коммитами

```bash
git status                   # статус изменений
git add --all                # добавить все файлы в отслеживание
git add .                    # добавить всё из текущей папки
git add fileName.txt         # добавить конкретный файл
git commit -m "Сообщение"    # создать коммит

git log                      # история коммитов
```

## 📊 Просмотр истории коммитов

```bash
git log --oneline
# Показывает историю коммитов в кратком виде (по одной строке на коммит)
# Удобно для просмотра ID коммитов (хешей)
```

## ♻️ Изменение последнего коммита

```bash
git commit --amend --no-edit
# Заменяет последний коммит без изменения сообщения
# Полезно, если ты забыл добавить файл:
#   git add важный_файл
#   git commit --amend --no-edit
```

```bash
git commit --amend -m "Новое сообщение"
# Заменяет последний коммит и позволяет изменить его сообщение
```

## 🔁 Работа с индексом (staging area)

```bash
git restore --staged <file>
# Убирает файл из индекса (отменяет git add), но не трогает содержимое
# Файл остаётся изменённым
```

```bash
git restore --staged .
# Убирает все файлы из индекса
```

## 🔄 Откат изменений в файле

```bash
git restore <file>
# Откатывает файл к последнему коммиту
# ⚠️ Несохранённые изменения будут удалены
```

## ⏮ Полный откат проекта

```bash
git reset --hard <commit_hash>
# Полностью откатывает проект к указанному коммиту
# ⚠️ Удаляет все коммиты и изменения после него
```

## 🔍 Просмотр отличий

## 🧾 Анализ истории конкретного файла

```bash
git log -p -- movies.txt
# Показывает все изменения (diff) в файле movies.txt за все коммиты
```

```bash
git diff <start_commit> <end_commit> -- movies.txt
# Показывает изменения между двумя конкретными коммитами в одном файле
# Пример:
git diff a0d292d 90c30e2 -- movies.txt
```

---

## 🔍 grep — поиск по выводу в терминале

`grep` позволяет фильтровать строки по шаблону. Очень мощный инструмент!

### 📌 Примеры:

```bash
git log -p -- movies.txt | grep '^+[^+]'
# Показывает только строки, добавленные в файл (без +++)
```

```bash
git log -p -- movies.txt | grep '^-'
# Показывает только строки, которые были удалены
```

```bash
cat file.txt | grep "Oscar"
# Найдёт все строки, содержащие слово "Oscar"
```

```bash
ps aux | grep java
# Покажет все процессы, связанные с Java
```

```bash
history | grep git
# Покажет все git-команды, которые ты вводил ранее
```

> ➕ grep работает практически в любой команде, где есть поток текста

```bash
git diff
# Показывает отличия между рабочими файлами и последним коммитом
```

```bash
git diff --staged
# Показывает отличия между индексом (staged) и последним коммитом
```

```bash
git diff <hash1> <hash2>
# Сравнивает два коммита по их хешам
```

## 🌐 Связь с удалённым репозиторием

```bash
git remote add origin git@github.com:Weltschmerzz/first-project.git  # задать удалённый репозиторий
git remote -v                                                  # список удалённых репозиториев

git push -u origin main     # пуш с установкой отслеживания
```

## 🛡 SSH-ключи

```bash
ls -la .ssh/                   # список ssh ключей в директории
ssh-keygen -t ed25519 -C email       # создать ssh ключ ed25519
ssh-keygen -t rsa -b 4096 -C email   # создать ssh ключ rsa (альтернатива)

ls -a ~/.ssh                   # убедиться, что ключи созданы
cat ~/.ssh/id_rsa.pub          # вывести публичный ключ
clip < ~/.ssh/id_ed25519.pub   # скопировать ключ в буфер обмена
ssh -T git@github.com          # проверить ssh-соединение с GitHub
```

> 🧠 При первом подключении появится сообщение о подлинности хоста.
> Проверка отпечатка ключа GitHub: https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/githubs-ssh-key-fingerprints

## 🔄 Клонирование репозитория

```bash
git clone git@github.com:yandex-praktikum/git-clone-lesson.git  # клонировать репозиторий

git remote -v   # убедиться, что связь с удалённым репозиторием установлена
```

---

> 💡 Этот конспект можно дополнять командами `branch`, `merge`, `checkout`, `stash`, и другими, когда будешь их изучать.

## 🌿 Работа с ветками

### 📌 Создание и управление ветками

```bash
git branch <имя_ветки>
# Создаёт новую локальную ветку
```

```bash
git checkout <имя_ветки>
# Переключиться на существующую ветку
```

```bash
git checkout -b <имя_ветки>
# Создать новую ветку и сразу на неё переключиться
```

> Ветка может содержать буквы, цифры, точки `.`, дефисы `-`, подчёркивания `_` и слэш `/`.
> Например: `feature/add-readme`.

---

### 📋 Просмотр веток

```bash
git branch
# Показывает все локальные ветки
```

```bash
git branch -a
# Показывает все локальные и удалённые ветки
```

---

### 🔀 Слияние веток

```bash
git merge <ветка>
# Вливает указанную ветку в текущую
```

---

### ⚔️ Разрешение конфликтов при merge

1. Открыть файл с конфликтом
2. Разрешить вручную
3. `git add file.txt`
4. `git commit`

---

### ❌ Удаление веток

```bash
git branch -d <ветка>    # Безопасное удаление
git branch -D <ветка>    # Принудительное удаление
```

---

### 🔍 Сравнение веток

```bash
git diff <ветка1> <ветка2>
```

---

### 🧭 Навигация по истории

```bash
HEAD~     # Один коммит назад
HEAD~2    # Два назад
```

---

### 🔗 Создание локальной ветки от удалённой

```bash
git checkout -b <локальная> origin/<удалённая>
```

---

## 🔗 Работа с удалённым репозиторием и ветками

### 🔍 Проверка и настройка

```bash
git remote -v
git remote show origin
```

```bash
git remote remove origin
git remote add origin git@github.com:user/repo.git
```

---

### 🚀 Push с отслеживанием

```bash
git push -u origin <ветка>
```

---

### ✅ Проверка связей

```bash
git branch -vv
```

## 🚀 Pull Request и командная работа

### 📌 Зачем нужен Pull Request (PR)

PR — это способ предложить изменения из одной ветки в другую (обычно из `feature/` в `main`), прежде чем они попадут в основную кодовую базу. Его можно обсудить, прокомментировать и протестировать.

---

### ✅ Как создать Pull Request

#### Способ 1: через терминал при первом пуше ветки

```bash
git push -u origin feature/merge-request
```

GitHub выведет ссылку вида:

```
remote: Create a pull request for 'feature/merge-request':
remote: https://github.com/UserName/project/pull/new/feature/merge-request
```

Её можно кликнуть или скопировать в браузер.

> ⚠️ Такая ссылка появляется только при первом пуше новой ветки.

---

#### Способ 2: вручную через GitHub

1. Перейти на страницу репозитория
2. Выбрать вкладку **Pull requests**
3. Нажать **New pull request**
4. Указать ветки: откуда → куда (например, `feature/...` → `main`)
5. Заполнить описание и нажать **Create pull request**

---

### 🔄 Что сделать перед PR: синхронизация с `main`

Если над проектом работают несколько человек, перед созданием PR **обязательно подтяни последние изменения из `main`**:

```bash
git checkout main
git pull
git checkout my-branch
git merge main
# или: git rebase main
git push
```

Это поможет:
- избежать конфликтов позже,
- упростить ревью,
- убедиться, что твои изменения не конфликтуют с актуальной версией проекта.

---

### 👥 Сценарий: Боря и Фёдор

1. Фёдор поработал в `main` и запушил изменения
2. Боря с пятницы ничего не делал

В понедельник Боря:

```bash
git checkout main
git pull                      # подтянуть обновления от Фёдора
git checkout feature/boris    # вернуться в свою ветку
git merge main                # влить изменения
# или: git rebase main
# решить конфликты, если есть
git push                      # обновить свою ветку
```

Теперь можно делать Pull Request!

## 🔄 Жизненный цикл файла в Git

Git отслеживает состояние каждого файла в репозитории. Состояния могут меняться в зависимости от действий пользователя:

- **`untracked`** — файл существует, но Git его не отслеживает (новый файл).
- **`staged`** — файл добавлен в список на коммит с помощью `git add`.
- **`tracked`** — файл уже закоммичен или хотя бы один раз был в staging.
- **`modified`** — файл изменён после коммита.

> ⚠️ Важно: если вы уже добавили файл в staging и потом снова его изменили — изменения нужно снова добавить с `git add`, иначе в коммит попадёт неактуальная версия.

---

## 🔑 Что такое хеш коммита (commit hash)

Каждый коммит в Git имеет уникальный **SHA-1 хеш** — это длинная строка, которая выглядит примерно так:

```
e3b0c44298fc1c149afbf4c8996fb92427ae41e4
```

Она:
- используется как идентификатор коммита,
- позволяет ссылаться на коммиты в других командах (`git checkout`, `git revert`, `git reset`),
- вычисляется на основе содержимого, времени, родительского коммита и автора.

---

## 🧭 Что такое HEAD

**`HEAD`** — это ссылка на **текущий коммит**, от которого ты сейчас «живёшь и работаешь». Чаще всего HEAD указывает на последнюю версию текущей ветки:

```bash
git show HEAD          # показать последний коммит
git log                # история коммитов
```

> Если ты создаёшь новый коммит — HEAD сдвигается вперёд.

---

## ✍️ Стиль сообщений к коммитам

### Хороший стиль:
- Кратко, по делу и в **повелительном наклонении**, как будто даёшь команду Git.
- На английском — в профессиональной среде это стандарт (если не указано иное).
- Без точки в конце.

#### Примеры:
```
Add login form validation
Fix typo in README
Refactor task manager logic
Update dependencies
```

> Можно добавить больше контекста через пустую строку и второй абзац:
```bash
git commit -m "Fix bug with null pointer" -m "Caused by missing check in TaskManager"
```

## 📥 echo и оператор перенаправления вывода

```bash
echo "строка текста" >> file.txt
# Добавляет строку в конец файла
```

```bash
echo "перезапись файла" > file.txt
# Перезаписывает файл, полностью удаляя старое содержимое
```

### Пример:

```bash
cat file.txt
# Первая строка файла

echo "Вторая строка файла" >> file.txt
cat file.txt
# Первая строка файла
# Вторая строка файла

echo "Новая строка" > file.txt
cat file.txt
# Новая строка
```

> ➕ `>>` — дописать в файл, `>` — перезаписать файл
